SELECT
    c.country,
    COUNT(a.customer_id) AS customer_count
FROM
    customer a
JOIN address b ON a.address_id = b.address_id
JOIN city d ON b.city_id = d.city_id
JOIN country c ON d.country_id = c.country_id
GROUP BY
    c.country
ORDER BY
    customer_count DESC;
